<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Play</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-0evHe/X+R7YkIZDRvuzKMRqM+OrBnVFBL6DOitfPri4tjfHxaWutUpFmBp4vmVor" crossorigin="anonymous">
    <style>
      body {    
          background-image: url("./image/Background.png");
          background-size: cover;
          background-repeat: no-repeat;
          background-position: center center;
          background-attachment: fixed;
      }
    </style>
  </head>
  <body>
    <p><a class="btn btn-success m-3" href="/landingpage">Back</a></p>
    <div class="position-absolute top-50 start-50 translate-middle text-center">
      
      <h1 class="fs-1 p-4" style="color:white ;">Create Forecast</h1> 
      
      <form action="/play" method="post">      
        <div class="row">
          <div class="col">
            <input type="number" name="home_goals" class="form-control" placeholder="Home goals" aria-label="home_goals">
          </div>
          <div class="col">
            <input type="number" name="visitor_goals" class="form-control" placeholder="Visitor goals" aria-label="visitor_goals">
          </div>
        </div>
      
        <p class="fs-3 mt-5" style="color:white ;">Match</p>
                          
        <select name="match_id" class="form-select " aria-label="Default select example">
          <% @matches = Match.all%>
          <% @matches.each do |m| %>
          <option value="<%= m.id %>"> <%= m.home_team.name %> vs <%= m.visitor_team.name %></option>
          <% end %>
        </select>
      
        <button type="submit" value="Create Forecast" class="btn btn-light m-3">Create Forecast</button>                  
      </form>
    </div>      
  </body>
</html>